<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.KuasCafeteria" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorOnPrimary">@color/accent_contrast</item>
        <item name="colorSurface">@color/background_primary</item>
        <item name="android:colorBackground">@color/background_tertiary</item>
        <item name="android:windowBackground">@color/background_tertiary</item>
        <item name="android:statusBarColor">@color/background_primary</item>
        <item name="android:navigationBarColor">@color/background_primary</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowAnimationStyle">@style/FadeActivityAnim</item>
        <item name="materialButtonStyle">@style/App.Button.Primary</item>
    </style>

    <!-- Filled buttons mirror web .btn-primary: text-primary fill,
         background-primary label, radius-md corners. -->
    <style name="App.Button.Primary" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/text_primary</item>
        <item name="android:textColor">@color/background_primary</item>
        <item name="iconTint">@color/background_primary</item>
        <item name="cornerRadius">@dimen/radius_md</item>
    </style>

    <style name="FadeActivityAnim" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/fade_in</item>
        <item name="android:activityOpenExitAnimation">@anim/fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/fade_out</item>
    </style>
</resources>
