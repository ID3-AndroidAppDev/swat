<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <!-- Sublabel (e.g. "LUNCH (1)") -->
    <TextView
        android:id="@+id/tv_sublabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="10sp"
        android:textColor="@color/text_tertiary"
        android:textStyle="bold"
        android:letterSpacing="0.07"
        android:textAllCaps="true"
        android:layout_marginBottom="2dp"
        android:visibility="gone"
        tools:text="LUNCH (1)"
        tools:visibility="visible" />

    <!-- Dish name -->
    <TextView
        android:id="@+id/tv_item_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="@color/text_primary"
        android:lineSpacingMultiplier="1.4"
        tools:text="Hokkaido Croquette &amp; Meat-Potato Croquette" />

    <!-- Price -->
    <TextView
        android:id="@+id/tv_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="13sp"
        android:textColor="@color/text_primary"
        android:textStyle="bold"
        android:visibility="gone"
        tools:text="¥430 · set ¥500"
        tools:visibility="visible" />

    <!-- Kcal + macros row -->
    <LinearLayout
        android:id="@+id/row_nutrition"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="4dp"
        android:visibility="gone">

        <!-- Kcal badge -->
        <TextView
            android:id="@+id/tv_kcal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="5dp"
            android:paddingEnd="5dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:textSize="10sp"
            android:textColor="@color/text_secondary"
            android:background="@color/background_secondary"
            android:layout_marginEnd="6dp"
            tools:text="468 kcal" />

        <TextView
            android:id="@+id/tv_protein"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="10sp"
            android:textColor="@color/text_tertiary"
            android:layout_marginEnd="6dp"
            tools:text="P 11g" />

        <TextView
            android:id="@+id/tv_fat"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="10sp"
            android:textColor="@color/text_tertiary"
            tools:text="F 25.6g" />
    </LinearLayout>

    <!-- Allergen chips container -->
    <LinearLayout
        android:id="@+id/chips_allergen"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="6dp" />

    <!-- Diet tags container -->
    <LinearLayout
        android:id="@+id/chips_diet"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="4dp" />

    <!-- Filtered-out overlay notice -->
    <TextView
        android:id="@+id/tv_filtered_notice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="10sp"
        android:textColor="@color/chip_allergen_fg"
        android:background="@drawable/bg_chip_allergen"
        android:paddingStart="5dp"
        android:paddingEnd="5dp"
        android:paddingTop="2dp"
        android:paddingBottom="2dp"
        android:visibility="gone" />
</LinearLayout>
